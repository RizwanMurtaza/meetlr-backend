namespace Meetlr.Domain.Enums;

public enum RecurrenceFrequency
{
    Daily = 1,
    Weekly = 2,
    BiWeekly = 3,
    Monthly = 4,
    Custom = 5 // For manually selected dates (bulk booking or custom schedule)
}
